# 2026-05-09 更新记录

## 背景

- 按需求为普客推荐页增加二级分类菜单，支持按厨具筛选料理结果。

## 变更范围

- 普客页新增厨具筛选：`src/pages/NormalPage.tsx`
  - 新增“全部 + 厨具”二级菜单（全部、煮锅、烧烤架、料理台、油锅、蒸锅）。
  - 料理结果在原有排序前先按厨具过滤；酒水列表不受影响。
  - 当地区切换导致当前厨具选项失效时，自动回退到“全部”。
  - 新增空结果提示：“当前厨具下没有可推荐料理”。

- 文档同步：`README.md`
  - 功能总览新增“普客页料理二级筛选”说明。
  - 使用教程新增“按厨具切换”步骤。

## 验证结果

- `pnpm lint`：通过。
- `pnpm build`：通过。
- 构建阶段仅出现既有的 chunk size warning，本次改动未引入新的构建错误。

## 风险与备注

- 本次筛选维度来自当前料理结果中的厨具字段；若未来新增料理使用新厨具，会自动出现在菜单中。
