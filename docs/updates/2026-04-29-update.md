# 2026-04-29 更新记录

## 背景

- 接入存档导入后的自动设置映射，让现有单标签流行模型与联动稀客显隐能直接消费存档信息。
- 本轮范围限定为：料理流行标签、`collabStatus`；酒水流行与 `rewindMode` 仅做兼容处理，不接入现有推荐与 UI。

## 变更范围

- 修正 `src/lib/special-customer-visibility.ts` 的 JSON 导入类型断言，避免 `pnpm build` 在严格类型检查下触发 TS2352。
- 更新 `docs/development-conventions.md`，明确推送到 GitHub 或发起 PR 前必须先在本地 `pnpm build` 通过。
- 扩展 `src/lib/save-parser.ts`：新增对 `popLikeFoodTags`、`popHateFoodTags`、`popLikeBevTags`、`popHateBevTags`、`collabStatus`、`rewindMode` 的防御式解析。
- 修正真实存档字段路径：上述字段优先从 `playerPartial` 读取，并兼容回退到 `storagePartial`。
- 临时关闭真实存档料理流行值的数字 tag id 映射，避免在枚举顺序未核实时把 `7` 之类的值错映射成错误标签。
- 修正导入互斥规则：确认流行喜好与流行厌恶可以同时存在，导入时不再互相清空。
- 新增 `src/lib/food-tags.ts`，统一可导入的料理标签集合，并复用到设置页与存档解析。
- 新增 `src/lib/special-customer-visibility.ts`，集中管理联动角色与 DLC9 角色的默认隐藏与按存档解锁逻辑。
- 更新 `src/stores/game-store.ts` 的 `importSave`：
  - 料理流行数组按“第一个有效值”写入 `popularFoodTag` 与 `popularHateFoodTag`
  - 流行喜好存在时，`popularHateFoodTag` 强制清空
  - 联动角色与 DLC9 角色默认隐藏
  - `collabStatus` 与 DLC 激活状态只驱动 `rareHiddenCustomerIds` 的解除隐藏
  - 保留非特殊来源的手动 `rareExtraCustomerIds`
- 已实现联动映射：`MC_Gensokyo -> [30]`、`3FARIES_Collab -> [31]`、`TBS_Kokoro -> [41]`、`TBC2_Collab -> [36, 37, 38]`
- `THYG` 作为合法空映射处理，不产生角色变化；未知 `collabStatus` key 继续忽略。
- 更新 `docs/tmi-cooking-mechanics-knowledge-base.md`，补充本轮导入语义与边界。
- 更新 `.playwright-cli/playwright-advanced-acceptance.fn.template.js`，改用 `Mystia#17.memory` 并显式断言导入后的热门料理标签互斥与特殊稀客展示。
- 修正设置页空值展示：流行料理标签空值从内部哨兵 `_none` 改为用户可读的“无”，并同步调整 Playwright 验收断言。
- 移除 `scripts/verify-rare-ingredient-availability.ts`，并从开发约定与同步流程中删除该校验步骤。
- 增强 `.playwright-cli/playwright-advanced-acceptance.fn.template.js`：新增非法配置导入提示、利润开关跨页生效、特殊稀客负向显隐、添加/移除稀客、稀客收藏与过滤放宽等更贴近真实操作的断言。

## 验证结果

- `pnpm lint`：通过。
- 使用真实 `Mystia#6.memory` 的定向解析校验：通过，当前可解析出 `popularFoodTag="西式"`，并导入 `rareExtraCustomerIds=[30]`。
- `bash scripts/run-playwright-advanced-acceptance.sh`：最终通过。
  - 产物目录：`.playwright-cli/tmp/2026-04-29_10:53/`
  - 导入样例存档成功，设置页显示 `喜爱=西式`、`厌恶=_none`
  - 稀客页检测到联动额外稀客“萌澄果”
  - 桌面与移动分辨率验收均完成，summary `errors=[]`
- 使用真实 `Mystia#17.memory` 的定向导入校验：通过。
  - 解析结果为 `popularFoodTag="和风"`、`popularHateFoodTag=null`
  - 即使导入前残留 `popularHateFoodTag="咸"`，导入后也会清空为 `null`
  - 导入后可见特殊稀客仅保留已开启联动对应的 `萌澄果` 与 `秦心`
  - 未开启联动角色与全部 DLC9 角色保持默认隐藏
- Playwright 设置页截图复核：通过。
  - 导入 `Mystia#17.memory` 后显示 `喜爱=和风`、`厌恶=无`
  - 不再向用户暴露内部哨兵值 `_none`
- Playwright 增强场景复核：通过。
  - 非法配置字符串可被明确拒绝
  - “料理利润计算”开启后会在普客页与稀客页显示利润，且跨页返回后状态保持
  - 已开启特殊稀客可见，未开启联动角色仍保持隐藏
  - “添加稀客 -> 移除” 与 “收藏菜品/酒水” 操作均成功完成
- 稀客食材可用性独立校验脚本已移除；后续不再要求执行这一步。
- 定向样例校验：通过。
  - `popLikeFoodTags: ["无效值", "辣", "甜"]` 最终导入为 `辣`
  - `popHateFoodTags: ["流行厌恶", "苦"]` 最终导入为 `苦`
  - `MC_Gensokyo=true` 与 `TBC2_Collab=true` 导入后追加 `[30, 36, 37, 38]`
  - `THYG=true` 不产生任何角色变化
  - 既有手动 `rareExtraCustomerIds=[9999]` 与 `rareHiddenCustomerIds=[31]` 在导入后仍被保留

## 风险与备注

- 本轮仍未把酒水流行接入现有产品能力；`rewindMode` 仅按无效字段兼容保留，未参与推荐、筛选或配置导出。
- 上游仓库能确认相关标签本体与动态标签用法，但没有公开 Mystia#x.memory 的保存枚举源；因此料理流行数字映射已临时禁用，待后续确认后再恢复。
- 若未来仓库补入 `THYG` 对应角色，需要同步更新映射表与知识库说明。
