# 2026-04-24 更新记录（明星店开关）

## 背景

- 需要在设置页增加“明星店”效果开关。
- 开启后，按夜雀助手逻辑让“招牌”标签额外触发“流行喜爱”。

## 变更范围

- 设置页：
- src/pages/SettingsPage.tsx
- 在“流行料理标签”区域新增“明星店效果”开关。

- 全局状态：
- src/stores/game-store.ts
- 新增 famousShopEnabled 与 setFamousShopEnabled。
- 配置导入时支持 famousShopEnabled（缺失默认 false）。

- 配置导入导出：
- src/lib/config-io.ts
- v6 配置新增可选字段 fs（famousShopEnabled）。

- 普客推荐：
- src/lib/normal-recommend.ts
- 明星店开启且料理含“招牌”时，额外加入“流行喜爱”。
- src/pages/NormalPage.tsx
- 将 famousShopEnabled 传入普客推荐计算。

- 稀客推荐：
- src/lib/tags.ts
- getDynamicTags 增加 isFamousShop 参数，开启时“招牌”触发“流行喜爱”。
- src/lib/rare-recommend.ts
- 透传 isFamousShop 到动态标签计算。
- src/pages/RarePage.tsx
- 将 famousShopEnabled 传入稀客推荐计算。

- 文档：
- docs/development-conventions.md
- 配置字段说明补充 fs（famousShopEnabled）。

## 验证结果

- pnpm lint：通过

## 风险与备注

- 当前实现为“叠加生效”：
- 若同时设置了常规流行标签与“明星店”开关，两者可同时使“流行喜爱”生效（标签去重后不会重复计分）。
